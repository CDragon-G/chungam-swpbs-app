// Placeholder. App boot requires Supabase init + .env, so a real smoke test
// needs a Riverpod ProviderScope with overrides. Add tests per-feature instead.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
